Mail Merge Helps you to send RFQs Easily by just inputting values in the macro excel files as outlook template files.
always keep/save the outlook template, attachments in the same directory where macro excel is saved.
keep the outlook template name 'message' same or else you need to change inside the vba.
